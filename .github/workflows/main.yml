name: pipeline

on:
  push:
    branches: 
      - master

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v1
    - name: Build and test
      run: |
        npm ci
        npm run build
    - name: Test
      run: |
        npm run test
