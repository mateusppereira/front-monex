name: pipeline

on:
  push:
    branches: 
      - master

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/setup-node@v1
    - name: Build
      run: |
        npm run build
        npm ci
  test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/setup-node@v1
    - name: Run tests
      run: npm run test
